
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div>
        @Html.ActionLink("添加", "Add", "useInfo")
        <hr/>
        <table border=" 1">
            <tr>
                <th>编号</th>
                <th>姓名</th>
                <th>密码</th>
                <th>修改</th>
                <th>删除</th>
            </tr>
          @foreach (var userinfo in Model)
          {
              <tr>
                  <td>@userinfo.UID</td>
                  <td>@userinfo.UName</td>
                  <td>@userinfo.UPassWord</td>
                  <td>@Html.ActionLink("修改","Edit","UseInfo",new { id= userinfo.UID},null)</td>
                  <td>@Html.ActionLink("删除", "Remove", "UseInfo", new { id = userinfo.UID }, null)</td>
              </tr>
          }
            @*@foreach (var name in Model)
            {
                <tr>
                    <td>@name</td>
                </tr>
            }*@
            @*@foreach (var userinfoviewmodel in Model)
    {
        <tr>
            <td>@userinfoviewmodel.Uid</td>
            <td>@userinfoviewmodel.Uname</td>

        </tr>
    }*@

        </table> 
    </div>
</body>
</html>
